/target

# macOS
.DS_Store
Icon?

# Claude Code local session data
.claude/

# scratch/manual-testing artifacts dropped at the repo root (not part of the project) —
# tests/fixtures/ has the .mod copies the test suite actually depends on
/*.mod
/*.MOD
/*.xm
/*.s3m
/*.vgm
/*.vgz
/*.als
/*.zip
/toto
/Samples/
/fichiers/

# generated Ableton projects (regenerate via `ablemod convert`, not source)
/PFANTAS1 Project/
/4aces-high Project/
/PINBALLF Project/
/Ship Project/
/Dream Project/
/pdreams/

# reference projects used to capture templates/group_track.xml, templates/audio_track.xml,
# templates/wavetable_track.xml, and templates/operator_track.xml — not needed once extracted
/Grouped Project/
/wav Project/
/wavetable Project/
/Operator Project/
